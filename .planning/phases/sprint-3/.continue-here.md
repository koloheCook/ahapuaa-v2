---
context: phase
phase: sprint-3
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-28T17:28:04Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| TILE_SCALE misdiagnosis | Changing TILE_SCALE to fix "hex row gap" made visible white border collision worse. The row overlap is the isometric 3D art style of the Kenney sprites -- no scale value fixes it. 54/96, 54/112, 54/116 all tested and rejected. 54/120 is correct (fits sprite width = cell width). | advisory | Do not touch TILE_SCALE. If the row overlap appearance is discussed, clarify it is art style. |

<current_state>
Sprint 3 execution is underway. Plans 01 and 02 are complete and committed. Plan 03 is in progress:
- T01 done (uncommitted): #tooltip div added to index.html
- T02 not started: pointermove tooltip handler, D-key toggle, friendlyReason function, expanded pointerdown rejection feedback -- all in GameScene.js

One uncommitted file: index.html (the #tooltip div addition).
</current_state>

<completed_work>

Sprint 3 progress:

**Plan 01 -- Tile Gap Polish (complete, ab2e9cb revert)**
- Changed TILE_SCALE to 54/96, browser-tested, confirmed it made overlap worse
- Reverted to 54/120 with corrected comment documenting isometric art style
- Decision logged: TILE_SCALE stays at 54/120; row overlap is art style

**Plan 02 -- Building Selector Panel (complete, 47212ca)**
- index.html: #selector-panel with type buttons (Hale, Loi, Loko-ia) and tier row (T1/T2/T3)
- GameScene.js: SINGLE_TIER_TYPES const at module level, updateSelector(scene) function, click handlers wired in create()
- Visual checkpoint passed by user: active state on load, Loi dims T2/T3, Hale re-enables, tier button selection all correct

**Plan 03 T01 -- #tooltip div (done, UNCOMMITTED)**
- index.html: #tooltip div added after #selector-panel, before script tag
- Style: position:fixed; display:none; pointer-events:none; z-index:100; matching HUD color language
</completed_work>

<remaining_work>

**Plan 03 T02 -- GameScene.js additions (not started)**

Four additions to GameScene.js per the plan spec:

ADDITION 1 (after updateSelector(this) init call, before pointerdown):
```js
const tooltipEl = document.getElementById('tooltip');
this.showRejectionText = true;
```

ADDITION 2 (new pointermove handler for tooltip -- after existing camera-pan pointermove):
- Convert pointer to world point, world to tile
- If out of bounds: tooltipEl.style.display = 'none'
- Build innerHTML: terrainType + Wet/Dry + building if any (e.g. "Hale T1")
- ADD col/row coordinates as extra line (user request for DevMode lookup)
- Position near pointer (pointer.x + 14, pointer.y + 14)
- pointerout handler: hide tooltip

ADDITION 3 (D-key toggle after tooltip handler):
```js
this.input.keyboard.on('keydown-D', () => {
  this.showRejectionText = !this.showRejectionText;
  if (!this.showRejectionText) document.getElementById('rejection-reason').style.opacity = '0';
});
```

ADDITION 4 (expand existing pointerdown else branch for canPlace failure):
- Red tint flash: img.setTint(0xff0000), this.time.delayedCall(300, restore original tint)
- Reason text: friendlyReason(reason) -> #rejection-reason, transition: none -> opacity: 1 -> rAF -> opacity: 0

MODULE-LEVEL: friendlyReason(reason) function after updateSelector -- maps canPlace reason strings to player-friendly text per the table in sprint-3-CONTEXT.md.

**After T02: Manual checkpoint**
Run localhost:5173 and verify:
1. Hover tooltip appears near cursor with terrain, Wet/Dry, (col,row), and building if placed
2. Tooltip hides on pointer leaving canvas
3. Mountain placement attempt: red tile flash + "Can't build on mountain" fades out
4. Loi dry tile: "Loi needs a wet tile"
5. D key toggles rejection text
6. grep -n "setTimeout" src/scenes/GameScene.js -> 0 results
7. git diff src/game/ -> empty

**Then: Create sprint-3-PLAN-03-SUMMARY.md + phase verification + DEVLOG.md update**
</remaining_work>

<decisions_made>

- **TILE_SCALE = 54/120 is correct** -- row overlap is isometric art style, not a bug. Do not change.
- **updateSelector is module-level** -- Plan 03 calls it as updateSelector(this) from create(); do not move to class method.
- **Add col/row to tooltip** -- user asked for DevMode tile coordinate lookup during Plan 02 checkpoint. Include (col, row) as a line in the hover tooltip content.
- **friendlyReason at module level** -- add after updateSelector; plan 03 spec says module-level.
</decisions_made>

<blockers>
None blocking code work. One human action pending: visual verification after Plan 03 T02.
</blockers>

## Required Reading (in order)

1. `src/scenes/GameScene.js` -- read the FULL current file before adding Plan 03 T02 additions; insertion points are relative to existing blocks (after updateSelector(this) init call, after camera-pan pointermove, etc.)
2. `.planning/phases/sprint-3/sprint-3-PLAN-03.md` -- full T02 spec with exact insertion instructions, friendlyReason table, and CSS transition pattern for rejection text
3. `.planning/phases/sprint-3/sprint-3-CONTEXT.md` -- canPlace reason -> display text mapping table
4. `CLAUDE.md` -- confirm no changes to src/game/ files; confirm this.time.delayedCall over setTimeout

## Critical Anti-Patterns (do NOT repeat these)

- **setTimeout for timed canvas effects**: Use `this.time.delayedCall(300, callback)` for the red tint restore. setTimeout is explicitly forbidden in CLAUDE.md and the plan spec.
- **Changing files in src/game/**: Plan 03 scope is GameScene.js and index.html only. canPlace, buildings.js, worldGen.js must not be touched.
- **innerHTML for rejection-reason**: Use `textContent` (not `innerHTML`) when inserting friendlyReason output into #rejection-reason to avoid XSS risk (per T-s3-05 in plan threat model).

## Infrastructure State

- **Dev server**: `npm run dev` on port 5173. Not currently running -- user will need to start it for the Plan 03 checkpoint.
- **Build**: `npm run build` was passing before this session; no structural changes expected.
- **Grep check**: `grep -r "from 'phaser'" src/game/` must always return empty (unchanged).
- **Uncommitted**: index.html (#tooltip div from T01)

<context>
Sprint 3 execution is 2/3 plans complete. Plan 03 is the final plan -- adds hover tooltip and rejection feedback. It's mostly a GameScene.js surgery with four distinct insertion points. The plan spec in sprint-3-PLAN-03.md is very detailed about WHERE each addition goes. Read GameScene.js in full first to get current line numbers (they shifted from Plan 02's additions). The tooltip should include col/row coordinates (user asked for DevMode lookup capability).
</context>

<next_action>
Start with: Read src/scenes/GameScene.js (full file) to orient yourself, then read sprint-3-PLAN-03.md T02 spec, then execute the four additions in order. index.html is already updated (T01 done -- #tooltip div present). Commit T01 and T02 together or separately per plan convention.
</next_action>
