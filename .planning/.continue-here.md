---
context: default
phase: sprint-3-prep
task: null
total_tasks: null
status: paused
last_updated: 2026-05-27T04:42:08Z
---

## Critical Anti-Patterns

_No blocking constraints discovered this session. Section retained for future entries._

<current_state>
Sprint 2 is complete and committed (ba5c471). The project is paused between
Sprint 2 and Sprint 3. The CLAUDE.md "Current Sprint" section is stale -- it
still describes Sprint 1 criteria and the May 19 demo deadline.

The immediate next task is: rewrite CLAUDE.md to reflect Sprint 2 complete,
define Sprint 3 scope, and optionally write SPRINT_3_PROMPT.md before executing.
</current_state>

<completed_work>

Sprint history:
- Sprint 1 (2026-05-18): Phaser 4.1.0 hex tilemap, 20x56 grid, all 6 terrain types.
  TILE_SCALE = 54/120. Committed: 87a5fe7
- Sprint 2 (2026-05-21): Camera pan/zoom, buildings.js + resourceTick.js ported,
  building placement with gold tint, resource HUD (7 fields), End Turn wired to
  processTick. Committed: ba5c471
- Post-bootcamp triage (2026-05-26): ahupuaa-v2 confirmed primary build, ahupuaa-game
  paused as reference prototype, DEVLOG updated, session-close skill standardized.
  Committed: 08772f8

This session (2026-05-21 -> 2026-05-27):
- Executed full Sprint 2 per SPRINT_2_PROMPT.md
- Verified all exit criteria via Playwright real-click testing
- Logged three Phaser 4.1.0 observed deltas in DEVLOG
- Created .claude/launch.json for dev server preview (untracked)
- .planning/ directory created this pause (previously did not exist)
</completed_work>

<remaining_work>

Immediate (before Sprint 3):
1. Rewrite CLAUDE.md "Current Sprint" section -- stale since Sprint 1. Must reflect:
   - Sprint 2 complete
   - Sprint 3 scope defined (see Sprint 3 goal below)
   - Remove May 19 demo deadline language (bootcamp is over)
   - Update "Do Not Build" list (Sprint 3 now in scope)

Sprint 3 scope (from DEVLOG 2026-05-26 entry and Sprint 2 DEVLOG):
- Building selector UI: HTML buttons to choose type (hale/loi/loko-ia) and tier
  (this.selectedType and this.selectedTier are hardcoded 'hale'/0 right now)
- Tile gap polish: Kenney sprites have transparent margin; slight gaps visible
  between hexes after TILE_SCALE. Consider tighter positioning or setOrigin tweak.
- Hover tooltip / debug overlay: show terrain type, coordinates, isWet, forestLevel
  on tile hover
- Visual feedback for rejected placements: brief red tint or shake animation
- Shore/water/forest tile improvements: replace placeholder tints with dedicated
  sprites when assets available
- Heiau building still blocked (no culturally appropriate asset -- DO NOT use
  medieval_church.png)

Not in scope until post-capstone:
- Procedural worldGen, Ike tech tree UI, save/load, mobile layout, full 12-turn
  Makahiki loop, rex-plugins, generateBirds(), Hoa Kaua integration
</remaining_work>

<decisions_made>

- **ahupuaa-v2 is canonical** -- ahupuaa-game paused as reference only (2026-05-26)
- **setOrigin default (center)** is correct for Phaser 4 -- tileToWorldXY returns
  pixel center; do not change to setOrigin(0,0)
- **state.tiles assigned in create()** -- `state.tiles = tiles` after buildWorld()
  exposes tile grid to End Turn handler without restructuring the state singleton
- **worldToTileXY confirmed Phaser 4.1.0** -- method exists, returns {x, y} plain
  object, no layer arg needed for hex tilemap

Unresolved:
- CLAUDE.md "Current Sprint" still describes Sprint 1. Must be rewritten.
- Heiau asset gap still open (culturally blocked, no replacement identified yet).
</decisions_made>

<blockers>
None blocking Sprint 3. One deferred item:
- **gh/1Password CLI**: `gh` alias requires interactive IO in Claude Code sessions.
  Workaround doc exists (fix prompt written 2026-05-19). Run the fix prompt in a
  separate session before any operation that needs `gh` from Claude Code.
</blockers>

## Required Reading (in order)

1. `DEVLOG.md` (first entry only -- 2026-05-26 post-bootcamp triage) -- current status
2. `CLAUDE.md` -- read before writing any code; note "Current Sprint" section is STALE
3. `PHASER_4_AUDIT.md` -- confirmed Phaser 4.1.0 deltas from Sprints 1+2
4. `src/scenes/GameScene.js` -- current scene state before any Sprint 3 changes

## Critical Anti-Patterns (do NOT repeat these)

- **Stale CLAUDE.md**: The "Current Sprint" section in CLAUDE.md still describes
  Sprint 1 exit criteria. Do NOT read it as authoritative for current sprint state.
  Read DEVLOG.md first entry for ground truth. Fix CLAUDE.md before starting Sprint 3.
- **No building selector yet**: `this.selectedType = 'hale'` and `this.selectedTier = 0`
  are hardcoded in GameScene. Clicking ANY tile attempts hale placement. Sprint 3 adds
  the selector UI -- do not confuse current click behavior with a complete system.

## Infrastructure State

- **Dev server**: `npm run dev` on port 5173 (defined in vite.config.js). Launch
  config at `.claude/launch.json` (untracked, port 5173).
- **Build**: `npm run build` passes clean (15 modules, no errors)
- **Grep check**: `grep -r "from 'phaser'" src/game/` must always return empty
- **GitHub repo**: koloheCook/ahapuaa-v2 (public). Push via terminal (gh CLI blocked
  in Claude Code -- see blockers above)
- **Reference prototype**: `../ahapuaa-game/` -- Phaser 3 prototype, paused at
  Sprint 2 complete. Use src/game/ files there as reference if porting is needed.

<context>
Sprint 2 shipped and verified. This pause is between sprints -- no in-flight work,
no partial tasks. The project is clean. The only friction going into Sprint 3 is the
stale CLAUDE.md (fix it first) and the absence of a Sprint 3 spec. Both are quick
writing tasks, not code tasks.

The Phaser 4 migration experiment has been clean -- zero P4-specific divergences in
Sprint 2. The codebase is healthy. Sprint 3 is primarily UI work (building selector)
and polish, not new game logic.
</context>

<next_action>
Start with: Read DEVLOG.md first entry (2026-05-26), then rewrite CLAUDE.md
"Current Sprint" section to reflect Sprint 2 complete and define Sprint 3 scope.
Then run Sprint 3 (or write SPRINT_3_PROMPT.md first if a spec doc is desired).
</next_action>
